{
  "name": "cypherpoet-toolchest-catalog",
  "description": "Validates the public CypherPoet Claude Code and Codex marketplace catalogs.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node scripts/check-catalogs.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "validate:claude": "claude plugin validate --strict ."
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "2.1.227"
  }
}
